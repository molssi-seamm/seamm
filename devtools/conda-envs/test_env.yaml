name: test

channels:
  - default

dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - jinja2
  - numpy
  - pandas
  - pillow
  - py-cpuinfo

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - bitmath
    - fasteners
    - Pmw
    - pyuca
    - reference-handler>=0.8.2
    - seamm-util>=0.9
    - seamm-widgets>=0.9
    - stevedore

      # Testing
    - codecov
    - yapf

