name: test

channels:
  - default

dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - numpy
  - pandas
  - pillow
  - py-cpuinfo

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - bitmath
    - Pmw
    - pyuca
    - reference-handler>=0.8
    - seamm-util>=0.8
    - seamm-widgets>=0.8
    - stevedore
      # Testing
    - yapf

