name: test

channels:
  - default

dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - numpy
  - pandas
  - pillow
  - py-cpuinfo

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - bitmath
    - Pmw
    - pyuca
    - seamm-util>=0.6
    - seamm-widgets
    - stevedore
      # Testing
    - yapf

